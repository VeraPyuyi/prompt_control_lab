# PromptControlLab curated quick-analysis demo

**Recommendation:** needs_review

The candidate mean score is `0.875`, compared with `0.625` for the baseline. The paired mean
delta is `0.25`, with bootstrap interval `[0.0, 0.5]` and permutation p-value `0.125`.

This eight-example fixture demonstrates the report path. It is not evidence of universal prompt
improvement. Repeat the protocol on a larger withheld task set before deployment.
